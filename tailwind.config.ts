module.exports = {
  darkMode: 'class', // This allows you to use the 'dark' class
  theme: {
    extend: {
      colors: {
        // Define custom colors if needed
      },
    },
  },
  plugins: [],
};
